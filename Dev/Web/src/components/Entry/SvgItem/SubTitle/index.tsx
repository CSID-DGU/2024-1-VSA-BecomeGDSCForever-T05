import * as Styled from "./style.ts";
import subTitle from "@/assets/titles/SubTitle.svg";

export default function SubTitle() {
    return (
        <Styled.Container src={subTitle} alt={"logo"} />
    )
}
